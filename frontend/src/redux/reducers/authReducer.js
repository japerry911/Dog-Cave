const INITIAL_STATE = {
  user: {},
  isAuthed: false,
  isLoading: false,
  error: null,
  token: null,
};

const authReducer = (state = INITIAL_STATE, action) => {
  switch (action.payload) {
  }
};

export default authReducer;
